# Local archive of the superseded 3D build — kept on disk, not shipped.
legacy-3d/

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Node — the Astro build and its tooling
node_modules/
npm-debug.log*
.wrangler/

# Astro's generated types and build cache
.astro/

# Astro's build output. `tools/publish.mjs` mirrors this to the repo root so the
# site can also be served straight from the repository; the artifact itself is
# regenerated by `npm run build` and does not need to be tracked.
out/

# Superseded planning documents
step-by-step-build-prompt.md
skills-lock.json
