# Local archive of the superseded 3D build — kept on disk, not shipped.
legacy-3d/

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Node (only used for linting / tooling, the game itself has no deps)
node_modules/
npm-debug.log*
.wrangler/
