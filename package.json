{
    "name": "guess-and-pass",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "description": "Guess & Pass — 3D players on a 2D top-view pitch. Decision-window football with the canonical §4 hybrid geometric + guess-bonus resolution algorithm.",
    "scripts": {
        "dev": "astro dev",
        "build": "astro build && node tools/publish.mjs",
        "build:only": "astro build",
        "preview": "astro preview",
        "verify": "node tools/verify-4.mjs",
        "deploy": "npm run build && wrangler pages deploy out --project-name guess-and-pass",
        "astro": "astro"
    },
    "dependencies": {
        "astro": "^5.0.0",
        "three": "0.149.0"
    },
    "devDependencies": {
        "wrangler": "^4.0.0"
    }
}